package com.nandan.interfaces;

public interface Brake {
    void brake();
    //does not matter as the class overrides for its own
    // so wheter here of at many interfaces it soes not matter
    //void start();
}
